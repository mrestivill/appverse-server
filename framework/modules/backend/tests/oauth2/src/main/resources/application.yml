spring:
  application:
    name: jdbc
  jersey:
    type: servlet
management:
  context_path: /admin
logging:
  level:
    org.springframework.security: DEBUG

---

spring:
  profiles: client
server:
  port: 8081  